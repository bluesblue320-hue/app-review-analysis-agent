# 2026-08-06 工作日志

## 项目：小红书评论分析 Agent 升级（一次启动连续 8 阶段）

主文档：`docs/app-review-analysis-final-upgrade-plan.md`。工作流：阶段 0-7 连续执行，每阶段独立提交 + 阶段报告（docs/execution/stage-N-report.md）。

### 阶段 0（CP0）
- 基线：145 passed + 3 subtests，覆盖率 64%。提交 `0eae65a`。

### 阶段 1（CP1）
- 评分 1~5 严格过滤、evidence_call_ids 服务端二次校验、PII 脱敏（backend/core/privacy.py）、Bearer 鉴权 + request ID（backend/core/middleware.py）、pyproject.toml/Ruff/pytest-cov/GitHub Actions。
- 180 passed + 3 subtests，覆盖率 70%。提交 `e62ffab`。

### 阶段 2（CP2）
- 双 Agent Adapter：`backend/agent/adapters/`（AgentModelAdapter Protocol + DirectDeepSeekAdapter + LangChainDeepSeekAdapter + build_adapter 工厂）。
- 共享 Orchestrator（tool_calling.py 改为接受统一 adapter），AGENT_ADAPTER=direct|langchain 运行时切换。
- LangChain 用 ChatDeepSeek.bind_tools + 自管有界循环（不用 Agent Executor）。
- 评估 CLI 增加 --adapter；Direct/LangChain Mock 均 46/46。
- 267 passed + 3 subtests，覆盖率 75.53%（门禁 75%）。
- **环境坑**：Windows"应用程序控制策略"阻止 uuid_utils 原生扩展 → langchain 依赖锁定 langchain-core==0.3.60 + langsmith==0.1.147 + langchain-deepseek==0.1.4 + langchain-openai==0.3.0。
- **Live 对比未执行**（无 DEEPSEEK_API_KEY、网络受限），如实登记为外部依赖阻塞，脚本已就绪，阶段 7 可补。
- 提交：`feat(agent): add Direct/LangChain dual adapters with shared orchestrator`。

### 阶段 3（CP3）
- Repository Protocol（DatasetRepository/InsightRepository）+ SQLAlchemy 2 持久化 + Alembic 初始迁移。
- insight_fingerprint UNIQUE 约束 + 原子 insert-or-get-existing（唯一冲突回滚后读已有记录，不 500）+ 过期同 ID 刷新。
- 范围签名统一：sha256(content_hash + canonical_filters + analysis_version)；修复 ai/analytics/agent 三服务签名不一致。
- /ready 端点；级联删除；重启恢复。
- 301 passed + 3 subtests，覆盖率 82%（门禁 75%）。
- 提交：`feat(storage): persist datasets reviews and insights with PostgreSQL`。

### 待办
- 阶段 4：Redis 分析缓存 + Insight 短锁。
- 阶段 7：补 Live 对比（需用户配置 DEEPSEEK_API_KEY）。
